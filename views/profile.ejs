<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="navbar flex justify-start w-full items-center gap-10">
            <img src="<%= user.profilepic %>" alt="">
            <h2 class="text-3xl mb-3"> <span class="font-light">Hello </span>, <%= user.name %> 👋</h2>
            <a class="mb-3 inline-block bg-red-500 px-3 py-2 rounded-md" href="/logout">logout 😢</a>
        </div>

        <p class="mt-12 text-zinc-500">Create a post</p>
        <form action="/post" method="post">
            <textarea class="mt-2 mb-2 w-1/3 p-4 bg-transparent border-zinc-700 border-2 resize-none block rounded-md"
                name="content" placeholder="What's on your mind ?"></textarea>
            <input type="submit" class="mt-3 inline-block bg-blue-500 px-3 py-2 rounded-md" value="Create Post" >
        </form>
        <h3 class="mt-20 text-zinc-500 mb-5">Your posts</h3>
        <div class="postcontainer  min-h-screen">
            <% user.posts.forEach(function(post){ %>
                <div class="mb-2  px-3 py-2 px-10 py-5 bg-transparent w-1/3 border-2 border-zinc-700 rounded-md">
                    <h3 class="text-blue-500 text-sm"><%= user.username %></h3>
                    <p class="mt-1  tracking-tighter mb-3"><%= post.content %></p>
                    <p class="text-sm font-light"><%= post.likes.length %> likes</p>
                    <a class="text-sm text-blue-500 mr-3" href="/like/<%= post._id %>"><%= (post.likes.indexOf(user._id) == -1) ? "Like" : "Unlike"%></a>
                    <a class="text-sm text-zinc-500" href="/edit">edit</a>
                </div>  
            <%  }) %>   

                   
        </div>
    </div>
</body>

</html>